const { pocScenario } = require('../scenarios/pocScenario')

const scenario = pocScenario

describe('login', () => {
    it ('Acessar página inicial e fazer login', async () => {
         await scenario
    } )
})